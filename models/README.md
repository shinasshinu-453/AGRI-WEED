# Place your YOLO model weights here
# 
# For the trained YOLOv11n model:
#   Copy your best.pt file to this directory:
#   models/best.pt
#
# This file is tracked by git, but model weights (*.pt) are gitignored.
# To upload model weights to Render, use one of:
#   1. Render Persistent Disk (recommended)
#   2. Git LFS (for files > 100MB)
#   3. Download from cloud storage on startup
